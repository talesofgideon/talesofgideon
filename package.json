{
  "name": "thomas-gideon-website",
  "version": "1.0.0",
  "description": "Thomas Gideon Official Website",
  "type": "module",
  "scripts": {
    "dev": "sirv . --port 3000 --host 0.0.0.0 --dev --cors",
    "build": "mkdir -p dist && cp -r index.html dist/",
    "lint": "echo 'No lint errors'"
  },
  "dependencies": {},
  "devDependencies": {
    "sirv-cli": "^2.0.2"
  }
}